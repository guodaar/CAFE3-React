import React from "react";
import TopBar from "../components/TopBar";

const Contacts = () => {
  return (
    <div>
      <TopBar />
      <h1>Contacts</h1>
    </div>
  );
};

export default Contacts;
